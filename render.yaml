services:
  - type: web
    name: loanshield
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir webapp app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
